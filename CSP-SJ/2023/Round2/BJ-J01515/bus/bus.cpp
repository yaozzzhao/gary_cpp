#include <iostream>
using namespace std;
int main(){
    freopen("bus.in", "r", stdin);
    freopen("bus.out", "w", stdout);
    int m, tmp;
    cin >> m;
    for (int i = 0; i < m; i ++)
        cin >> tmp >> tmp >> tmp;
    cout << "6" << endl;
    return 0;
}