#include<iostream>
#include<cstdio>
#include<cmath>
#include<algorithm>
#include<string>
using namespace std;
char iuput[50], output[50];
int char2int(char ch){
	return (ch >= '0' && ch <= '9') ? (ch - '0') : (10 + ch - 'A');
}
char int2char(int in){
	return (a >= 0 && a <= 9) ? (a + '0') : (a - 10 + 'A');
} 
int x2ten(char num[], int x){
	int n = strlen(num);
	int res = 0;
	for(int i = 0; i < n; i++){
		res = res * x +
	}
}
int main(){
	int n, m;
	cin >> n >> input >> m;
	
	return 0;
}